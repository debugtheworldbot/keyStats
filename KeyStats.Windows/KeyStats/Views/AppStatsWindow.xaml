<Window x:Class="KeyStats.Views.AppStatsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:viewmodels="clr-namespace:KeyStats.ViewModels"
        xmlns:p="clr-namespace:KeyStats.Properties"
        Title="{x:Static p:Strings.AppStats_WindowTitle}"
        Width="720"
        Height="640"
        MinWidth="640"
        MinHeight="520"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource WindowSurfaceBrush}"
        ShowInTaskbar="True">

    <Window.DataContext>
        <viewmodels:AppStatsViewModel/>
    </Window.DataContext>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="{x:Static p:Strings.AppStats_HeaderTitle}" FontSize="20" FontWeight="SemiBold"
                       Foreground="{DynamicResource TextPrimaryBrush}"/>
            <TextBlock Text="{x:Static p:Strings.AppStats_HeaderSubtitle}"
                       FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}"
                       TextWrapping="Wrap" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Range -->
        <Border Grid.Row="1" Background="{DynamicResource SubtleFillBrush}" CornerRadius="6" Padding="3" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <RadioButton Content="{x:Static p:Strings.AppStats_RangeToday}" GroupName="AppStatsRange"
                             Style="{DynamicResource SegmentedButton}" FontSize="11" Padding="10,4"
                             IsChecked="{Binding SelectedRangeIndex, Converter={StaticResource IntToBoolConverter}, ConverterParameter=0}"/>
                <RadioButton Content="{x:Static p:Strings.AppStats_Range7Days}" GroupName="AppStatsRange"
                             Style="{DynamicResource SegmentedButton}" FontSize="11" Padding="10,4"
                             IsChecked="{Binding SelectedRangeIndex, Converter={StaticResource IntToBoolConverter}, ConverterParameter=1}"/>
                <RadioButton Content="{x:Static p:Strings.AppStats_Range30Days}" GroupName="AppStatsRange"
                             Style="{DynamicResource SegmentedButton}" FontSize="11" Padding="10,4"
                             IsChecked="{Binding SelectedRangeIndex, Converter={StaticResource IntToBoolConverter}, ConverterParameter=2}"/>
                <RadioButton Content="{x:Static p:Strings.AppStats_RangeAll}" GroupName="AppStatsRange"
                             Style="{DynamicResource SegmentedButton}" FontSize="11" Padding="10,4"
                             IsChecked="{Binding SelectedRangeIndex, Converter={StaticResource IntToBoolConverter}, ConverterParameter=3}"/>
            </StackPanel>
        </Border>

        <!-- Summary -->
        <TextBlock Grid.Row="2" Text="{Binding SummaryText}" Style="{DynamicResource SecondaryText}"
                   Margin="0,0,0,8"
                   Visibility="{Binding HasData, Converter={StaticResource BoolToVisibilityConverter}}"/>

        <!-- Column Header -->
        <Grid Grid.Row="3" Margin="0,0,0,6"
              Visibility="{Binding HasData, Converter={StaticResource BoolToVisibilityConverter}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="120"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{x:Static p:Strings.AppStats_ColumnApp}" FontSize="12" FontWeight="SemiBold"
                       Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center"/>
            <Button Grid.Column="1" Content="{Binding KeysHeader}" Style="{DynamicResource AppStatsHeaderButton}"
                    Command="{Binding SortCommand}" CommandParameter="Keys"/>
            <Button Grid.Column="2" Content="{Binding ClicksHeader}" Style="{DynamicResource AppStatsHeaderButton}"
                    Command="{Binding SortCommand}" CommandParameter="Clicks"/>
            <Button Grid.Column="3" Content="{Binding ScrollHeader}" Style="{DynamicResource AppStatsHeaderButton}"
                    Command="{Binding SortCommand}" CommandParameter="Scroll"/>
        </Grid>

        <!-- List -->
        <Grid Grid.Row="4">
            <TextBlock Text="{Binding EmptyText}" FontSize="13"
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVisibilityConverter}}"/>

            <Border Visibility="{Binding HasData, Converter={StaticResource BoolToVisibilityConverter}}"
                    Background="Transparent" BorderThickness="0">
                <ListBox ItemsSource="{Binding AppStatsItems}"
                         Background="Transparent"
                         BorderThickness="0"
                         Padding="0"
                         AlternationCount="2"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0"/>
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Focusable" Value="False"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <ContentPresenter/>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>

                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="6,4" Margin="0,2" CornerRadius="6">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="{DynamicResource CardBrush}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=(ItemsControl.AlternationIndex)}" Value="1">
                                                <Setter Property="Background" Value="{DynamicResource SubtleFillBrush}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="120"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- App Column -->
                                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                        <Image Source="{Binding Icon}" Width="16" Height="16" Margin="4,0,6,0"
                                               Visibility="{Binding HasIcon, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                        <TextBlock Text="{Binding DisplayName}" FontSize="12"
                                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                                   TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                                    </StackPanel>

                                    <!-- Keys Bar -->
                                    <Grid Grid.Column="1" Margin="6,0">
                                        <Border x:Name="KeysTrack" Background="{DynamicResource SubtleFillBrush}"
                                                CornerRadius="4" Height="18" VerticalAlignment="Center">
                                            <Grid>
                                                <Border x:Name="KeysFill" Background="{DynamicResource AppStatsKeysBrush}"
                                                        CornerRadius="4" HorizontalAlignment="Left" ClipToBounds="True">
                                                    <Border.Width>
                                                        <MultiBinding Converter="{StaticResource BarWidthConverter}">
                                                            <Binding Path="KeysRatio"/>
                                                            <Binding ElementName="KeysTrack" Path="ActualWidth"/>
                                                        </MultiBinding>
                                                    </Border.Width>
                                                    <TextBlock Text="{Binding KeysText}" FontSize="10" Foreground="White"
                                                               HorizontalAlignment="Right" Margin="0,0,4,0"
                                                               VerticalAlignment="Center"
                                                               Visibility="{Binding KeysLabelInside, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                                </Border>
                                                <TextBlock Text="{Binding KeysText}" FontSize="10"
                                                           Foreground="{DynamicResource TextSecondaryBrush}"
                                                           HorizontalAlignment="Right" Margin="0,0,4,0"
                                                           VerticalAlignment="Center"
                                                           Visibility="{Binding KeysLabelOutside, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                            </Grid>
                                        </Border>
                                    </Grid>

                                    <!-- Clicks Bar -->
                                    <Grid Grid.Column="2" Margin="6,0">
                                        <Border x:Name="ClicksTrack" Background="{DynamicResource SubtleFillBrush}"
                                                CornerRadius="4" Height="18" VerticalAlignment="Center">
                                            <Grid>
                                                <Border x:Name="ClicksFill" Background="{DynamicResource AppStatsClicksBrush}"
                                                        CornerRadius="4" HorizontalAlignment="Left" ClipToBounds="True">
                                                    <Border.Width>
                                                        <MultiBinding Converter="{StaticResource BarWidthConverter}">
                                                            <Binding Path="ClicksRatio"/>
                                                            <Binding ElementName="ClicksTrack" Path="ActualWidth"/>
                                                        </MultiBinding>
                                                    </Border.Width>
                                                    <TextBlock Text="{Binding ClicksText}" FontSize="10" Foreground="White"
                                                               HorizontalAlignment="Right" Margin="0,0,4,0"
                                                               VerticalAlignment="Center"
                                                               Visibility="{Binding ClicksLabelInside, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                                </Border>
                                                <TextBlock Text="{Binding ClicksText}" FontSize="10"
                                                           Foreground="{DynamicResource TextSecondaryBrush}"
                                                           HorizontalAlignment="Right" Margin="0,0,4,0"
                                                           VerticalAlignment="Center"
                                                           Visibility="{Binding ClicksLabelOutside, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                            </Grid>
                                        </Border>
                                    </Grid>

                                    <!-- Scroll Bar -->
                                    <Grid Grid.Column="3" Margin="6,0">
                                        <Border x:Name="ScrollTrack" Background="{DynamicResource SubtleFillBrush}"
                                                CornerRadius="4" Height="18" VerticalAlignment="Center">
                                            <Grid>
                                                <Border x:Name="ScrollFill" Background="{DynamicResource AppStatsScrollBrush}"
                                                        CornerRadius="4" HorizontalAlignment="Left" ClipToBounds="True">
                                                    <Border.Width>
                                                        <MultiBinding Converter="{StaticResource BarWidthConverter}">
                                                            <Binding Path="ScrollRatio"/>
                                                            <Binding ElementName="ScrollTrack" Path="ActualWidth"/>
                                                        </MultiBinding>
                                                    </Border.Width>
                                                    <TextBlock Text="{Binding ScrollText}" FontSize="10" Foreground="White"
                                                               HorizontalAlignment="Right" Margin="0,0,4,0"
                                                               VerticalAlignment="Center"
                                                               Visibility="{Binding ScrollLabelInside, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                                </Border>
                                                <TextBlock Text="{Binding ScrollText}" FontSize="10"
                                                           Foreground="{DynamicResource TextSecondaryBrush}"
                                                           HorizontalAlignment="Right" Margin="0,0,4,0"
                                                           VerticalAlignment="Center"
                                                           Visibility="{Binding ScrollLabelOutside, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                            </Grid>
                                        </Border>
                                    </Grid>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>
        </Grid>
    </Grid>
</Window>
