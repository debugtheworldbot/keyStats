<Window x:Class="KeyStats.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:p="clr-namespace:KeyStats.Properties"
        Title="{x:Static p:Strings.Settings_WindowTitle}"
        Width="420" SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{DynamicResource WindowSurfaceBrush}"
        ShowInTaskbar="True">

    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled">
      <StackPanel Margin="24,20">
        <Grid Margin="0,0,0,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="{x:Static p:Strings.Settings_HeaderTitle}" FontSize="16" FontWeight="SemiBold"
                       Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>

            <Button Grid.Column="1" Click="OpenGitHub_Click"
                    Width="28" Height="28" Padding="0" Margin="8,0,0,0"
                    Background="Transparent" BorderThickness="0" Cursor="Hand"
                    ToolTip="{x:Static p:Strings.Settings_GitHubTooltip}">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Width="{TemplateBinding Width}"
                                Height="{TemplateBinding Height}"
                                Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
                <Viewbox Width="16" Height="16" Stretch="Uniform">
                    <Canvas Width="16" Height="16">
                        <Path Fill="{DynamicResource TextSecondaryBrush}"
                              Data="M8,0.2C3.58,0.2 0,3.78 0,8.2C0,11.74 2.29,14.74 5.47,15.8C5.87,15.87 6.02,15.63 6.02,15.43C6.02,15.25 6.01,14.64 6.01,13.83C3.78,14.31 3.31,12.76 3.31,12.76C2.95,11.85 2.42,11.61 2.42,11.61C1.69,11.11 2.48,11.12 2.48,11.12C3.29,11.18 3.72,11.95 3.72,11.95C4.43,13.17 5.59,12.82 6.05,12.62C6.12,12.1 6.33,11.74 6.56,11.53C4.78,11.33 2.91,10.64 2.91,7.57C2.91,6.69 3.22,5.98 3.73,5.43C3.65,5.23 3.37,4.41 3.81,3.3C3.81,3.3 4.48,3.08 6.01,4.12C6.65,3.94 7.33,3.85 8,3.85C8.67,3.85 9.35,3.94 9.99,4.12C11.52,3.08 12.19,3.3 12.19,3.3C12.63,4.41 12.35,5.23 12.27,5.43C12.78,5.98 13.09,6.69 13.09,7.57C13.09,10.65 11.21,11.33 9.43,11.53C9.72,11.78 9.98,12.27 9.98,13.02C9.98,14.09 9.97,15.17 9.97,15.43C9.97,15.63 10.11,15.88 10.52,15.8C13.71,14.74 16,11.74 16,8.2C16,3.78 12.42,0.2 8,0.2Z" />
                    </Canvas>
                </Viewbox>
            </Button>
        </Grid>
        <TextBlock Text="{x:Static p:Strings.Settings_HeaderSubtitle}"
                   Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12"
                   TextWrapping="Wrap" Margin="0,0,0,16"/>

        <Border Style="{DynamicResource CardBorder}" Margin="0,0,0,8" Padding="14,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="{x:Static p:Strings.Settings_DataImportExport}" FontSize="13" FontWeight="SemiBold"
                               Foreground="{DynamicResource TextPrimaryBrush}"/>
                    <TextBlock Text="{x:Static p:Strings.Settings_DataImportExportDesc}" FontSize="12"
                               Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,4,0,0"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button x:Name="ImportDataButton" Content="{x:Static p:Strings.Common_Import}" Style="{DynamicResource GhostButton}"
                            MinWidth="70" Margin="0,0,6,0" Click="ImportData_Click"/>
                    <Button Content="{x:Static p:Strings.Common_Export}" Style="{DynamicResource GhostButton}"
                            MinWidth="70" Click="ExportData_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Style="{DynamicResource CardBorder}" Margin="0,0,0,8" Padding="14,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="{x:Static p:Strings.Settings_Sync}" FontSize="13" FontWeight="SemiBold"
                               Foreground="{DynamicResource TextPrimaryBrush}"/>
                    <TextBlock x:Name="SyncStatusTextBlock" Text="{x:Static p:Strings.Settings_SyncDesc}" FontSize="12"
                               Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,4,10,0" TextWrapping="Wrap"/>
                </StackPanel>
                <Button Grid.Column="1" Content="{x:Static p:Strings.Common_Open}" Style="{DynamicResource GhostButton}"
                        MinWidth="70" Click="SyncSettings_Click"/>
            </Grid>
        </Border>

        <Border Style="{DynamicResource CardBorder}" Margin="0,0,0,8" Padding="14,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="{x:Static p:Strings.Settings_Notifications}" FontSize="13" FontWeight="SemiBold"
                               Foreground="{DynamicResource TextPrimaryBrush}"/>
                    <TextBlock Text="{x:Static p:Strings.Settings_NotificationsDesc}" FontSize="12"
                               Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,4,0,0"/>
                </StackPanel>
                <Button Grid.Column="1" Content="{x:Static p:Strings.Common_Open}" Style="{DynamicResource GhostButton}"
                        MinWidth="70" Click="NotificationSettings_Click"/>
            </Grid>
        </Border>

        <Border Style="{DynamicResource CardBorder}" Margin="0,0,0,8" Padding="14,12">
            <StackPanel>
                <TextBlock Text="{x:Static p:Strings.Settings_FloatingStats}" FontSize="13" FontWeight="SemiBold"
                           Foreground="{DynamicResource TextPrimaryBrush}"/>
                <TextBlock Text="{x:Static p:Strings.Settings_FloatingStatsDesc}" FontSize="12"
                           Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,4,0,0"
                           TextWrapping="Wrap"/>

                <Grid Margin="0,10,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0"
                               Text="{x:Static p:Strings.FloatingStats_PrimaryMetric}"
                               FontSize="12"
                               Foreground="{DynamicResource TextPrimaryBrush}"
                               VerticalAlignment="Center"/>
                    <ComboBox x:Name="FloatingPrimaryMetricComboBox"
                              Grid.Row="0" Grid.Column="1"
                              MinWidth="150"
                              DisplayMemberPath="Label"
                              SelectedValuePath="Id"
                              SelectionChanged="FloatingMetric_SelectionChanged"/>

                    <TextBlock Grid.Row="1"
                               Text="{x:Static p:Strings.FloatingStats_SecondaryMetric}"
                               FontSize="12"
                               Foreground="{DynamicResource TextPrimaryBrush}"
                               VerticalAlignment="Center"
                               Margin="0,8,0,0"/>
                    <ComboBox x:Name="FloatingSecondaryMetricComboBox"
                              Grid.Row="1" Grid.Column="1"
                              MinWidth="150"
                              DisplayMemberPath="Label"
                              SelectedValuePath="Id"
                              SelectionChanged="FloatingMetric_SelectionChanged"
                              Margin="0,8,0,0"/>

                    <TextBlock Grid.Row="2"
                               Text="{x:Static p:Strings.FloatingStats_Layout}"
                               FontSize="12"
                               Foreground="{DynamicResource TextPrimaryBrush}"
                               VerticalAlignment="Center"
                               Margin="0,8,0,0"/>
                    <ComboBox x:Name="FloatingLayoutComboBox"
                              Grid.Row="2" Grid.Column="1"
                              MinWidth="150"
                              SelectionChanged="FloatingLayout_SelectionChanged"
                              Margin="0,8,0,0">
                        <ComboBoxItem Content="{x:Static p:Strings.FloatingStats_SingleRow}" Tag="singleRow"/>
                        <ComboBoxItem Content="{x:Static p:Strings.FloatingStats_DoubleRow}" Tag="doubleRow"/>
                    </ComboBox>

                    <TextBlock Grid.Row="3"
                               Text="{x:Static p:Strings.FloatingStats_FontSize}"
                               FontSize="12"
                               Foreground="{DynamicResource TextPrimaryBrush}"
                               VerticalAlignment="Center"
                               Margin="0,8,0,0"/>
                    <ComboBox x:Name="FloatingFontSizeComboBox"
                              Grid.Row="3" Grid.Column="1"
                              MinWidth="150"
                              SelectionChanged="FloatingFontSize_SelectionChanged"
                              Margin="0,8,0,0"/>
                </Grid>

                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                    <CheckBox x:Name="FloatingTopmostCheckBox"
                              Content="{x:Static p:Strings.FloatingStats_AlwaysOnTop}"
                              Checked="FloatingStatsBehavior_Changed"
                              Unchecked="FloatingStatsBehavior_Changed"
                              Margin="0,0,18,0"/>
                    <CheckBox x:Name="FloatingLockPositionCheckBox"
                              Content="{x:Static p:Strings.FloatingStats_LockPosition}"
                              Checked="FloatingStatsBehavior_Changed"
                              Unchecked="FloatingStatsBehavior_Changed"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <Border Style="{DynamicResource CardBorder}" Margin="0,0,0,4" Padding="14,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="{x:Static p:Strings.Settings_DistanceCalibration}" FontSize="13" FontWeight="SemiBold"
                               Foreground="{DynamicResource TextPrimaryBrush}"/>
                    <TextBlock Text="{x:Static p:Strings.Settings_DistanceCalibrationDesc}" FontSize="12"
                               Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,4,0,0"/>
                </StackPanel>
                <Button Grid.Column="1" Content="{x:Static p:Strings.Common_Open}" Style="{DynamicResource GhostButton}"
                        MinWidth="70" Click="MouseCalibration_Click"/>
            </Grid>
        </Border>

        <Border Style="{DynamicResource CardBorder}" Margin="0,0,0,4" Padding="14,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="{x:Static p:Strings.Settings_Language}"
                               FontSize="13" FontWeight="SemiBold"
                               Foreground="{DynamicResource TextPrimaryBrush}"/>
                    <TextBlock Text="{x:Static p:Strings.Settings_LanguageDescription}"
                               FontSize="12"
                               Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,4,0,0"/>
                </StackPanel>
                <ComboBox Grid.Column="1"
                          x:Name="LanguageComboBox"
                          MinWidth="140"
                          SelectionChanged="LanguageComboBox_SelectionChanged"
                          Loaded="LanguageComboBox_Loaded">
                    <ComboBoxItem Content="{x:Static p:Strings.Settings_Language_System}" Tag="system"/>
                    <ComboBoxItem Content="简体中文" Tag="zh-Hans"/>
                    <ComboBoxItem Content="繁體中文" Tag="zh-Hant"/>
                    <ComboBoxItem Content="English" Tag="en"/>
                </ComboBox>
            </Grid>
        </Border>

        <TextBlock x:Name="VersionTextBlock"
                   Margin="2,10,0,0"
                   FontSize="11"
                   Foreground="{DynamicResource TextSecondaryBrush}"/>

      </StackPanel>
    </ScrollViewer>
</Window>
